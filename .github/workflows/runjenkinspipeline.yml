name: Trigger Jenkins Pipeline

on:
  workflow_dispatch:  # This enables manual triggering from the GitHub UI

jobs:
  trigger-jenkins:
    runs-on: ubuntu-latest  # Runs on a GitHub-hosted Ubuntu runner
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Trigger Jenkins Pipeline
        run: |
          curl -X POST https://6cd7-49-206-62-19.ngrok-free.app/job/myfirstpipeline/build \
          --user "gowthamdronamraju:11eb3dd54a12638df3a2bc7cf630dd8835"

